# Salin file ini jadi .env untuk development lokal (vercel dev akan membacanya).
# Ambil connection string dari Neon dashboard > Connection Details > Pooled connection.
DATABASE_URL=postgresql://user:password@ep-xxxx-pooler.region.aws.neon.tech/dbname?sslmode=require
